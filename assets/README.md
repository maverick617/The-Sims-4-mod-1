# Assets

Put original editable assets here.

Recommended folders:

- `textures/` for PNG, PSD, XCF, or other editable texture files
- `meshes/` for Blender or mesh source files
- `icons/` for original icon artwork

Only add files that you have the right to publish.
