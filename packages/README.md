# Packages

Put built `.package` files here for testing and releases.

Recommended practice:

- Commit small release-ready packages only when useful.
- Keep local experiments in `packages/local/`, which is ignored by Git.
- Add final downloads to GitHub Releases so players can find them easily.
